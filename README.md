# sqd | A SQL-like document management system

Traditional Unix tools (grep, sed, awk) are powerful but have inconsistent syntax and hard learning curves. sqd provides a familiar SQL interface for common text operations.